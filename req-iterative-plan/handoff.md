# Current handoff — Phase 10

- **Status:** Phases 0–10 complete. Phase 11 (pm.sendRequest auxiliary requests with callbacks) is next; Promises/deadlines polish (12), imports (13–16), variable persistence (17), output modes (18) and delivery (19) remain.
- **Repository:** D:\Projects\Work\M\GoPM, branch main. Phase 10 is the single commit directly on top of 370ef95 (Phase 9), message "Phase 10: tests and supported assertions", working tree clean; this handoff accompanies that commit and no remote action was performed.
- **Changed implementation:** New internal/scripting/assertions.go (`expectPrelude` JS shim behind pm.expect, `installAssertions` wiring pm.test/pm.expect, `testErrorText`/`firstLine`). engine.go gained TestResult{Name, Failed, Error} and Report.Tests; loop.go gained the per-run `tests` slice (reset each run, included in skip/success reports). bindings.go: pm allowlist +expect/test, pm.response allowlist +to, `pm.response.to.have.status(code)` via guarded proxies. execution: codeAssertions=6 (run.go), runPhase prints PASS/FAIL lines and returns assertFailed, RunLifecycle applies preferAssertions (6 > 3 > 4 > 0; 2/5/130 untouched) at five exit sites. cli: exitAssertions constant.
- **Public interfaces:** scripting: Report{Logs, Tests []TestResult, Skipped}; TestResult{Name, Failed, Error}; everything else unchanged (NewEngine, ExecRequest, ResponseData, SetPreRequest/SetPostRequest). execution: RunLifecycle/Prepare/Execute/MergeOverrides/ResolveRequest signatures unchanged. CLI surface unchanged.
- **Behavior:** pm.test(name, fn) records named outcomes and continues on failure (any thrown error → failed test with the Error's bare `message`); a pre-phase failed test never cancels the HTTP send. pm.expect chains: to/be/have/and words, not/deep flags, equal, a/an (typeof + array/null), property(name[,value]), include (string/array/object-subset), lengthOf, true/false/null/undefined getters; strict === semantics unless deep; unsupported chain words (e.g. .exist) raise "unsupported assertion" instead of silently passing. pm.response.to.have.status(code) = numeric status assertion. Async pm.test callbacks rejected explicitly (callback returned a Promise → script failure, no premature pass). Outcome lines: `ID: PASS name` / `ID: FAIL name: msg` on stderr; exit 6 when any test failed, with 130 > 7 > 5 > 6 > 3 > 4 precedence (5-beats-6 and 6-beats-4/3/0 verified). pm.sendRequest still raises the compatibility error.
- **Verification:** 147 test functions pass (`go test -count=1 ./...`, exit 0; 7 new: TestAssertionsContinue, TestAssertionChains, TestAssertionFailures, TestAssertionDeepEqual, TestAsyncTestRejected, TestResponseStatusAssertion in internal/scripting/assertions_test.go, cli TestRunAssertions; plus the two pm-allowlist pins in bindings_test.go updated); `go test -race -count=1 ./...` exit 0; vet/build/gofmt clean; `git diff --check` clean. Loopback servers only; Windows.
- **Decisions/limits:** All Phase 10 semantics recorded in docs/decisions.md (JS shim rationale, proxy-trapped chains, strict/deep fixture semantics, catch-everything pm.test, message extraction, exit-6 precedence). Deferred: async pm.test support and pm.sendRequest (11–12), structured/JSON output of test results (18); `.eql`/`.exist`/other Chai words deliberately unsupported and loud.
- **Next action:** Read phases/phase-11-async-callbacks.md and refine against internal/scripting/loop.go (startHTTP/fetch are the spike-era helpers to grow into pm.sendRequest: URL-object/method/headers/body conversion, body limits, 20-request/4-concurrency caps, generation-checked completions) and bindings.go (add sendRequest to the pm allowlist; callbacks settle through the owner-goroutine job queue — HTTP workers never touch runtime values). Assertions interact with phase 11: tests recorded after async work settles must still aggregate (runPhase reads them from the final Report) and async test callbacks stay rejected until Phase 12 decides otherwise.
