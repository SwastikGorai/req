# Current handoff — Phase 15

- **Status:** Phases 0–15 are complete and verified. Phase 16 (cURL export) is next; variable persistence, structured output/downloads and final delivery remain.
- **Repository:** `D:\Projects\Work\M\GoPM`, branch `main`, baseline commit `d169c07` (`Phase 14: imported Postman scripts`). Phase 15 changes are intentionally uncommitted; no remote action was performed.
- **Implementation:** `internal/importer/curl.go` tokenizes one POSIX-like `curl` command without a shell, supports quoting/escaping/line continuations, maps the required cURL flags into one native request, and stores source provenance. `ImportCurl` parses fully before reusing `Workspace.CreateRequest` for `--save-as` persistence. Imported `@file` body/form references remain untrusted and are not read.
- **Safety/behavior:** Active pipelines, redirections, command separators, substitutions, variable/pathname/tilde expansions, unknown flags, URL-list files, stdin references, unsupported form attributes, unsupported body combinations and multiple URLs fail before writing. Repeated data joins with `&`; JSON pieces join directly; `-G` appends raw data to the existing query; `-u` maps basic auth; `-L` and `-k` persist explicit redirect/TLS policy. cURL without `-L` runs with no redirects, while CLI `--no-follow` still overrides an imported `-L` request.
- **Changed files:** `internal/importer/curl.go`, `internal/importer/curl_test.go`, `internal/cli/import.go`, `internal/cli/curl_import_test.go`, `internal/cli/root.go`, `internal/cli/run.go`, `internal/model/types.go`, `docs/compatibility.md`, `docs/decisions.md`, `req-iterative-plan/PLAN.md`, `req-iterative-plan/phases/index.md`, `req-iterative-plan/phases/phase-15-curl-import.md`, and `req-iterative-plan/handoff.md`.
- **Verification:** A temporary writable `GOCACHE` was removed after verification. Focused cURL tests passed with explicit timeouts: importer `TestCurl` (9), CLI `TestCurlImport` (3), and model/execution packages (25). The importer/CLI package subset passed 94 tests. `rtk go test -count=1 -timeout=180s ./...` and `rtk go test -race -count=1 -timeout=240s ./...` each passed 251 tests in 9 packages. `rtk go vet ./...`, `rtk gofmt -l internal cmd`, and `rtk git diff --check` passed cleanly. Any later code edits require rerunning these gates.
- **Assumptions and limits:** `--save-as` targets an existing collection/folder path, matching `request create` without `--parents`; cURL file references retain their source-relative spelling and provenance but require remapping before execution. `--data @file` warns because native streaming cannot perform cURL's newline/NUL stripping without reading at import; strict mode rejects it. `--data-urlencode`, header files, form `<file`, stdin, URL-list files and other flags outside the fixed subset are intentionally rejected. `--json` uses the native JSON body mode and therefore validates inline JSON at import/run boundaries, a deliberate safety tightening over cURL's permissive JSON pass-through.
- **Next action:** Review this uncommitted Phase 15 diff, self-audit for speculative aliases/helpers, commit it with the established `Phase 15: ...` wording, then begin [phase-16-curl-export.md](phases/phase-16-curl-export.md). Do not add export behavior while reviewing this phase.
