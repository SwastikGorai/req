# Current handoff — Phase 11

- **Status:** Phases 0–11 complete. Phase 12 (Promises, deadlines and cleanup) is next; imports (13–16), variable persistence (17), output modes (18) and delivery (19) remain.
- **Repository:** `D:\Projects\Work\M\GoPM`, branch `main`; Phase 11 changes are uncommitted and no remote action was performed.
- **Changed implementation:** `internal/scripting/async.go` adds callback-only `pm.sendRequest` conversion and scheduling. `loop.go` tracks a 20-request FIFO with at most 4 workers, cancellation and generation-checked owner-thread delivery. `bindings.go` exposes `pm.sendRequest`; callback responses reuse the existing response adapter. Tests cover token acquisition before the main send, nested callbacks, callback throws, object conversion, caps and lifecycle integration.
- **Behavior:** Accepts string URLs or supported request objects (`url`, `method`, headers, raw/urlencoded/text formdata bodies, basic/bearer auth). Variables resolve when scheduled; auxiliary requests inherit no main headers/auth. Responses are limited to 10 MiB. Promise form and formdata file access remain unsupported for Phase 12/later.
- **Verification:** `rtk go test -count=1 -timeout=120s ./...` passed (209 tests, 8 packages); `rtk go test -race -count=1 -timeout=180s ./...` passed (209 tests, 8 packages); `rtk go vet ./...` and `rtk git diff --check` passed on Windows using loopback HTTP servers.
- **Next action:** Read `phases/phase-12-async-promises.md`; extend the same owner loop with Promise settlement, deadline and cleanup behavior without duplicating callback request conversion.
