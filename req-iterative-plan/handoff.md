# Current handoff — Phase 8

- **Status:** Phases 0–8 complete. Phase 9 is next; pm variables/request/response bindings, assertions, auxiliary requests and imports (Phases 9–19) remain incomplete.
- **Repository:** D:\Projects\Work\M\GoPM, branch main. Phase 8 is commit fd2b61d on top of f31145e (post-Phase-7 review fixes); this handoff accompanies that commit and no remote action was performed.
- **Changed implementation:** New internal/execution/lifecycle.go, internal/cli/script.go and tests for both; internal/scripting production bindings (pm.execution.skipRequest, console) with the spike surface moved to spike_bindings_test.go; Report.Skipped; dispatch extracted from Execute; store.UpdateScripts; launchEdit extracted from editJSON; run flags --no-scripts/--script-timeout; root.go script routing and help update.
- **Public interfaces:** execution exposes ScriptPolicy{Disabled, Timeout}, InheritedScripts(coll, segments) (pre, post, err), RunLifecycle(ctx, saved, ov, pol, sp, pre, post, stdout, stderr) int, DefaultScriptTimeout (5s), MaxScriptBodyBytes (10 MiB); Execute/Prepare unchanged; exit code 5 added. store exposes UpdateScripts(ctx, path, *model.Scripts, expected Revision). scripting.NewEngine() installs only pm.execution.skipRequest + console (no spike globals); Report{Logs, Skipped}. CLI: `req script edit PATH (--pre|--post)`.
- **Behavior:** Pre scripts (collection → outer-to-inner folders → request, enabled entries only, per-entry 5s deadline) run before final variable resolution; pre error = exit 5 with no send; skipRequest = exit 0, no send/post; HTTP error status still runs post scripts; transport failure does not; post error = exit 5 with body still printed; script error 5 beats --fail 4; cancellation 130. With post scripts present the body is buffered up to 10 MiB (over-limit: exit 5, no post scripts, no partial output); otherwise it streams as before. Script edit uses `// ---- req script <ID> ----` separators; markers must survive edits unchanged; the other phase, enabled flags and provenance are untouched; invalid edits are preserved in recovery; --send does not take the script flags (direct sends have no scripts).
- **Verification:** 126 test functions pass (`go test -count=1 ./...`, exit 0), including 21 new Phase 8 tests; `go test -race -count=1 ./...` exit 0; vet/build exit 0; gofmt clean after normalizing CRLF endings the subagent introduced; `git diff --check` clean. Loopback servers only; Windows.
- **Decisions/limits:** Response-buffer limit is a constant (configurability deferred with a ponytail note); console is uncapped until Phase 9; override-merge-before-pre-scripts is unobservable until Phase 9 exposes pm.request (Prepare is still one pass; split it when scripts can inspect the request); skipRequest aborts the current script via a catchable sentinel but the skip flag always wins, and phase enforcement (pre only) lives in the lifecycle, not the engine; no engine is created when no scripts will run. See docs/decisions.md.
- **Next action:** Read phases/phase-9-script-bindings.md and refine against scripting.NewEngine, variables.Scope layers (CLI > local > environment > collection) and the buffered response in RunLifecycle. Bind pm.variables/environment/collectionVariables get/set/has/unset/replaceIn, pre-request header mutation and raw body access, response code/status/headers/text/json/time (post phase only, clear error in pre), bounded console, and descriptive unknown-API errors; keep CLI overrides highest priority and script mutations on the execution copy only. Preserve the launchEdit/dispatch seams.
