# Current handoff — Phase 12

- **Status:** Phases 0–12 complete; Phase 13 (Postman collection and environment import) is next. Imports (13–16), variable persistence (17), output modes (18) and delivery (19) remain.
- **Repository:** `D:\Projects\Work\M\GoPM`, branch `main`, baseline commit `44beb5a` (`Phase 11: auxiliary requests with callbacks`); Phase 12 changes are uncommitted and no remote action was performed.
- **Changed implementation:** `internal/scripting/async.go` accepts callback and Promise `pm.sendRequest` forms through the same request parser/scheduler. Promise resolve/reject closures and callback invocations run only on the owner goroutine. `internal/scripting/loop.go` flushes Goja Promise jobs after host completions, tracks handled/unhandled rejections, cancels child contexts on failures, suppresses canceled/late deliveries and clears pending work at run end. `internal/scripting/bindings.go` keeps the regular response compatibility guard while allowing the `then` probe required when a response is a Promise fulfillment value.
- **Behavior:** One-argument `pm.sendRequest` returns a Promise resolving to the existing response adapter; network and auxiliary body-limit failures reject. Handled rejections continue; unhandled/re-thrown rejections fail the script. Async IIFEs and nested Promise chains can set variables before the main request. Existing callback requests retain their behavior. Script context deadlines cover auxiliary HTTP; skip, cancellation and runtime failure cancel active work and suppress late callbacks/reactions. Auxiliary requests remain bounded at 20 total, 4 concurrent and 10 MiB responses; logs remain bounded at 1000 entries/1 MiB. Timers/modules are not exposed, so calls produce source-located ReferenceErrors.
- **Changed files:** `internal/scripting/async.go`, `internal/scripting/bindings.go`, `internal/scripting/loop.go`, `internal/scripting/phase12_test.go`, `internal/execution/lifecycle_test.go`, `req-iterative-plan/PLAN.md`, `req-iterative-plan/phases/index.md`, `req-iterative-plan/phases/phase-12-async-promises.md`, `docs/decisions.md`.
- **Verification:** `rtk go test -count=1 -timeout=120s ./internal/scripting ./internal/execution` passed (112 tests); `rtk go test -count=1 -timeout=120s ./...` passed (220 tests, 8 packages); `rtk go test -race -count=1 -timeout=180s ./internal/scripting` passed (91 tests); `rtk go test -race -count=1 -timeout=180s ./...` passed (220 tests, 8 packages); repeated cancellation `rtk go test -race -count=10 -timeout=180s -run 'Test(AsyncCancelDisposesPromiseResult|SkipWithPendingPromiseCancelsHTTP|AuxiliaryDeadlineCancelsHTTP)' ./internal/scripting` passed (30 tests); `rtk go vet ./...` and `rtk git diff --check` exited 0. `rtk gofmt -w` was run on all changed Go files.
- **Next action:** Parent integrator should review and commit the Phase 12 changes using the established `Phase 12: ...` wording, then begin Phase 13 task 1 in `phases/phase-13-postman-data.md`.
- **Unresolved:** None known; Phase 13 was not started. Existing design intentionally does not claim hard JS heap isolation, and timers/modules remain absent as required.
