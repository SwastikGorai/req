# Current handoff — Phase 16

- **Status:** Phases 0–16 are complete and verified. Phase 17 (persist extracted variables safely) is next; structured output/downloads and final delivery remain.
- **Repository:** `D:\Projects\Work\M\GoPM`, branch `main`, baseline commit `9c9372a` (`Phase 15: cURL command import`). The Phase 16 changes listed below are intentionally uncommitted; no remote action was performed. This corrects the stale Phase 15 handoff, which incorrectly called Phase 15 changes uncommitted and pointed at the Phase 14 commit.
- **Implementation:** `internal/exporter/curl.go` formats one native saved request as a POSIX-safe cURL command. It quotes every argument with single-quote escaping, retains placeholders and body/form file references without opening files, and maps method, query, headers, inherited auth, body modes, redirect policy and TLS policy. `req export curl PATH` warns on enabled inherited/request scripts and other nonrepresentable behavior; `--strict` returns before stdout. `--resolve --env NAME` is the only substitution path and uses the selected environment plus collection values. `--form-string` was added to the importer for literal multipart round-trip.
- **Changed files:** `internal/exporter/curl.go`, `internal/exporter/curl_test.go`, `internal/cli/export.go`, `internal/cli/export_test.go`, `internal/cli/root.go`, `internal/importer/curl.go`, `docs/compatibility.md`, `docs/decisions.md`, `req-iterative-plan/PLAN.md`, `req-iterative-plan/phases/index.md`, `req-iterative-plan/phases/phase-16-curl-export.md`, and `req-iterative-plan/handoff.md`.
- **Verification:** Focused `rtk go test -count=1 ./internal/exporter ./internal/cli -run TestCurl` passed. `rtk go test -count=1 -timeout=180s ./...` and `rtk go test -race -count=1 -timeout=240s ./...` passed all packages. `rtk go vet ./...`, `rtk gofmt -l internal cmd`, and `rtk git diff --check` passed.
- **Assumptions and limits:** Default export intentionally keeps unresolved placeholders and literal saved file paths; cURL itself does not resolve req placeholders. Native relative files resolve from the workspace root, while exported cURL must run from that corresponding base; imported untrusted source references may need remapping first. `--resolve` fails on missing variables rather than emitting a misleading command and may expose credentials in stdout. Unsupported body/auth tags, blocked import metadata, invalid headers and ambiguous multipart file paths are warnings in lenient mode and strict no-write failures. Scripts are reported but never executed.
- **Next action:** Begin [phase-17-variable-persistence.md](phase-17-variable-persistence.md), task 1. Do not add Phase 17 persistence behavior while reviewing this phase.
