# Current handoff — Phase 14

- **Status:** Phases 0–14 are complete and verified. Phase 15 (cURL command import) is next; cURL export, variable persistence, output modes and delivery remain.
- **Repository:** `D:\Projects\Work\M\GoPM`, branch `main`, baseline commit `f3b4610` (`Phase 13: Postman collection and environment import`). Phase 14 changes are intentionally uncommitted; no remote action was performed.
- **Implementation:** Phase 13's native importer already stores Postman `prerequest`/`test` events as ordered pre/post scripts with joined source lines, enabled state, IDs and provenance. Phase 14 reuses that model and `execution.InheritedScripts` so enabled entries run collection → outer folder → inner folder → request. `RunLifecycle` now labels Goja sources with imported provenance (`postman:<JSON path>`), retaining script IDs for native/unprovenanced entries.
- **Runtime behavior:** Imported source is never executed during parsing/import. A saved login fixture proves callback and Promise `pm.sendRequest` forms settle through the existing owner-loop scheduler, mutate execution-local variables, and run before the main request. Obvious `pm.globals`, `pm.iterationData`, timer and module calls remain import warnings only; strict mode rejects them before writing, lenient mode preserves them and runtime guards/reference errors fail loudly if reached.
- **Changed files:** `internal/execution/lifecycle.go`, `internal/importer/postman_test.go`, `internal/importer/testdata/postman-login.json`, `internal/importer/testdata/postman-unsupported-runtime.json`, `internal/cli/postman_scripts_test.go`, `docs/compatibility.md`, `docs/decisions.md`, `req-iterative-plan/PLAN.md`, `req-iterative-plan/phases/index.md`, `req-iterative-plan/phases/phase-14-postman-scripts.md`, and `req-iterative-plan/handoff.md`.
- **Verification:** Importer focused (`13` passed), CLI Postman focused (`5` passed), runtime diagnostic (`1` passed), strict runtime warning/no-write (`1` passed), full suite (`239` passed in 9 packages), full race suite (`239` passed in 9 packages), `rtk go vet ./...` with a writable temporary Go cache (exit 0), `rtk git diff --check` (exit 0), and `rtk gofmt -l internal cmd` (no output). Every Go test used an explicit timeout; the temporary cache was removed afterward.
- **Next action:** Review the uncommitted Phase 14 diff, commit it with the established `Phase 14: ...` wording, then begin Phase 15 task 1 from [phase-15-curl-import.md](phases/phase-15-curl-import.md). Do not add cURL work while reviewing this phase.
- **Assumptions and limits:** Static detection is deliberately heuristic, not complete JavaScript analysis. Runtime compatibility remains authoritative; unsupported APIs are not silently emulated. No Phase 15 implementation was added, and no known unresolved issue remains.
